export type CountryName = {
  common: string;
  nativeName: {
    msa: {
      common: string;
      official: string;
    };
  };
  official: string;
};

export type CountryCurrencies = {
  ANG: {
    name: string;
    symbol: string;
  };
};
